<?xml version="1.0"?>
<project name="datatype-ncpdp" basedir="." default="build">

	<dirname property="plugin.dir" file="${ant.file.datatype-ncpdp}"/>
	<property file="${plugin.dir}/../build.properties" />

	<property name="classes" value="${basedir}/classes" />
	<property name="src" value="${basedir}/src" />
	<property name="dist" value="${server.extensions}/datatype-ncpdp" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>

	<target name="init">
		<mkdir dir="${classes}" />
		<mkdir dir="${dist}" />
	</target>

	<target name="compile" depends="init">
		<javac srcdir="${src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<copy todir="${dist}" file="${src}/plugin.xml" />
		<jar destfile="${dist}/datatype-ncpdp-shared.jar" basedir="${classes}">
			<exclude name="com/mirth/connect/plugins/datatypes/ncpdp/NCPDPDataTypeServerPlugin.class" />
		</jar>
		<jar destfile="${dist}/datatype-ncpdp-server.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/datatypes/ncpdp/NCPDPDataTypeServerPlugin.class" />
		</jar>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/datatype-ncpdp-${version}.zip" basedir="${server.extensions}" includes="datatype-ncpdp/**/*" />
	</target>

</project>
