<?xml version="1.0"?>
<project name="dicom-connector" basedir="." default="build">

	<!-- Load shared plugin properties -->
	<dirname property="plugin.dir" file="${ant.file.dicom-connector}"/>
	<property file="${plugin.dir}/../build.properties" />

	<!-- Local properties -->
	<property name="classes" value="${basedir}/classes" />
	<property name="src.connector" value="${server.src}/com/mirth/connect/connectors/dimse" />
	<property name="src.dcm4che2" value="${server.src}/org/dcm4che2" />
	<property name="lib" value="${server.lib.extensions}/dimse" />
	<property name="dist" value="${server.extensions}/dicom" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>

	<target name="init">
		<mkdir dir="${classes}" />
		<mkdir dir="${dist}" />
	</target>

	<target name="compile" depends="init">
		<javac srcdir="${server.src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<include name="com/mirth/connect/connectors/dimse/" />
			<include name="org/dcm4che2/" />
			<include name="com/mirth/connect/server/util/DICOMMessageUtil.java" />
			<include name="com/mirth/connect/server/userutil/DICOMUtil.java" />
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<!-- Copy plugin metadata XML files -->
		<copy todir="${dist}">
			<fileset dir="${src.connector}">
				<include name="*.xml" />
			</fileset>
		</copy>

		<!-- Copy extension libraries -->
		<copy todir="${dist}/lib" failonerror="false" quiet="true">
			<fileset dir="${lib}" />
		</copy>

		<!-- Create shared jar (classes needed by client) -->
		<jar destfile="${dist}/dicom-shared.jar" basedir="${classes}">
			<include name="com/mirth/connect/connectors/dimse/DICOMReceiverProperties.class" />
			<include name="com/mirth/connect/connectors/dimse/DICOMDispatcherProperties.class" />
		</jar>

		<!-- Create server jar -->
		<jar destfile="${dist}/dicom-server.jar" basedir="${classes}">
			<include name="com/mirth/connect/connectors/dimse/" />
			<include name="org/dcm4che2/" />
			<include name="com/mirth/connect/server/userutil/DICOMUtil.class" />
			<include name="com/mirth/connect/server/util/DICOMMessageUtil.class" />
			<exclude name="com/mirth/connect/connectors/dimse/DICOMReceiverProperties.class" />
			<exclude name="com/mirth/connect/connectors/dimse/DICOMDispatcherProperties.class" />
		</jar>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/dicom-${version}.zip" basedir="${server.extensions}" includes="dicom/" />
	</target>

</project>
