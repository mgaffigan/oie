<?xml version="1.0"?>
<project name="tcp-connector" basedir="." default="build">

	<dirname property="plugin.dir" file="${ant.file.tcp-connector}"/>
	<property file="${plugin.dir}/../build.properties" />

	<property name="src" value="${basedir}/src" />
	<property name="classes" value="${basedir}/classes" />
	<property name="dist" value="${server.extensions}/tcp" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>

	<target name="compile">
		<mkdir dir="${classes}" />
		<javac srcdir="${src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<mkdir dir="${dist}" />
		<copy todir="${dist}" file="${src}/source.xml" />
		<copy todir="${dist}" file="${src}/destination.xml" />
		<copy todir="${dist}" file="${src}/plugin.xml" />
		<jar destfile="${dist}/tcp-shared.jar" basedir="${classes}">
			<include name="com/mirth/connect/connectors/tcp/TcpReceiverProperties.class" />
			<include name="com/mirth/connect/connectors/tcp/TcpDispatcherProperties.class" />
			<include name="com/mirth/connect/connectors/tcp/TcpConnectorServletInterface.class" />
		</jar>
		<jar destfile="${dist}/tcp-server.jar" basedir="${classes}">
			<exclude name="com/mirth/connect/connectors/tcp/TcpReceiverProperties.class" />
			<exclude name="com/mirth/connect/connectors/tcp/TcpDispatcherProperties.class" />
			<exclude name="com/mirth/connect/connectors/tcp/TcpConnectorServletInterface.class" />
		</jar>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/tcp-${version}.zip" basedir="${server.extensions}" includes="tcp/" />
	</target>

</project>
