<?xml version="1.0"?>
<project name="datatype-xml" basedir="." default="build">

	<dirname property="plugin.dir" file="${ant.file.datatype-xml}"/>
	<property file="${plugin.dir}/../build.properties" />

	<property name="src" value="${basedir}/src" />
	<property name="classes" value="${basedir}/classes" />
	<property name="dist" value="${server.extensions}/datatype-xml" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
		<fileset dir="${client.lib}" includes="**/*.jar" />
		<pathelement location="${client.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>

	<target name="compile">
		<mkdir dir="${classes}" />
		<javac srcdir="${src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<mkdir dir="${dist}" />
		<copy todir="${dist}" file="${src}/plugin.xml" />
		<jar destfile="${dist}/datatype-xml-shared.jar" basedir="${classes}">
			<exclude name="com/mirth/connect/plugins/datatypes/xml/XMLDataTypeServerPlugin.class" />
			<exclude name="com/mirth/connect/plugins/datatypes/xml/XMLDataTypeClientPlugin.class" />
		</jar>
		<jar destfile="${dist}/datatype-xml-server.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/datatypes/xml/XMLDataTypeServerPlugin.class" />
		</jar>
		<jar destfile="${dist}/datatype-xml-client.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/datatypes/xml/XMLDataTypeClientPlugin.class" />
		</jar>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/datatype-xml-${version}.zip" basedir="${server.extensions}" includes="datatype-xml/**/*" />
	</target>

</project>
