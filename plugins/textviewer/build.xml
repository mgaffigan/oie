<?xml version="1.0"?>
<project name="textviewer" basedir="." default="build">

	<!-- Load shared plugin properties -->
	<dirname property="plugin.dir" file="${ant.file.textviewer}"/>
	<property file="${plugin.dir}/../build.properties" />

	<!-- Local properties -->
	<property name="src.plugin" value="${server.src}/com/mirth/connect/plugins/textviewer" />
	<property name="lib" value="${server.lib.extensions}/textviewer" />
	<property name="dist" value="${server.extensions}/textviewer" />

	<target name="clean">
		<!-- No compiled classes to clean -->
	</target>

	<target name="init">
		<mkdir dir="${dist}" />
	</target>

	<target name="build" depends="init">
		<!-- Copy plugin metadata XML files -->
		<copy todir="${dist}">
			<fileset dir="${src.plugin}">
				<include name="*.xml" />
			</fileset>
		</copy>

		<!-- Copy extension libraries -->
		<copy todir="${dist}/lib" failonerror="false" quiet="true">
			<fileset dir="${lib}" />
		</copy>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/textviewer-${version}.zip" basedir="${server.extensions}" includes="textviewer/**/*" />
	</target>

</project>
