<?xml version="1.0"?>
<project name="scriptfilestep-plugin" basedir="." default="build">

	<dirname property="plugin.dir" file="${ant.file.scriptfilestep-plugin}"/>
	<property file="${plugin.dir}/../build.properties" />

	<property name="src" value="${basedir}/src" />
	<property name="classes" value="${basedir}/classes" />
	<property name="dist" value="${server.extensions}/scriptfilestep" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>

	<target name="compile">
		<mkdir dir="${classes}" />
		<javac srcdir="${src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<mkdir dir="${dist}" />
		<copy todir="${dist}" file="${src}/plugin.xml" />
		<jar destfile="${dist}/scriptfilestep-shared.jar" basedir="${classes}" />
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/scriptfilestep-${version}.zip" basedir="${server.extensions}" includes="scriptfilestep/" />
	</target>

</project>
