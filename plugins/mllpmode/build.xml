<?xml version="1.0"?>
<project name="mllpmode-plugin" basedir="." default="build">

	<dirname property="plugin.dir" file="${ant.file.mllpmode-plugin}"/>
	<property file="${plugin.dir}/../build.properties" />

	<property name="src" value="${basedir}/src" />
	<property name="classes" value="${basedir}/classes" />
	<property name="dist" value="${server.extensions}/mllpmode" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>
	
	<target name="compile">
		<mkdir dir="${classes}" />
		<javac srcdir="${src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<mkdir dir="${dist}" />
		<copy todir="${dist}" file="${src}/plugin.xml" />
		<jar destfile="${dist}/mllpmode-shared.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/mllpmode/MLLPModeProperties.class" />
		</jar>
		<jar destfile="${dist}/mllpmode-server.jar" basedir="${classes}">
			<exclude name="com/mirth/connect/plugins/mllpmode/MLLPModeProperties.class" />
		</jar>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/mllpmode-${version}.zip" basedir="${server.extensions}" includes="mllpmode/" />
	</target>

</project>
