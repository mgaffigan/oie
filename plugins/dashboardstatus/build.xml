<?xml version="1.0"?>
<project name="dashboardstatus-plugin" basedir="." default="build">

	<!-- Load shared plugin properties -->
	<dirname property="plugin.dir" file="${ant.file.dashboardstatus-plugin}"/>
	<property file="${plugin.dir}/../build.properties" />

	<!-- Local properties -->
	<property name="classes" value="${basedir}/classes" />
	<property name="src.plugin" value="${server.src}/com/mirth/connect/plugins/dashboardstatus" />
	<property name="lib" value="${server.lib.extensions}/dashboardstatus" />
	<property name="dist" value="${server.extensions}/dashboardstatus" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>

	<target name="init">
		<mkdir dir="${classes}" />
		<mkdir dir="${dist}" />
	</target>

	<target name="compile" depends="init">
		<javac srcdir="${server.src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<include name="com/mirth/connect/plugins/dashboardstatus/" />
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<!-- Copy plugin metadata XML files -->
		<copy todir="${dist}">
			<fileset dir="${src.plugin}">
				<include name="*.xml" />
			</fileset>
		</copy>

		<!-- Copy extension libraries -->
		<copy todir="${dist}/lib" failonerror="false" quiet="true">
			<fileset dir="${lib}" />
		</copy>

		<!-- Create shared jar (classes needed by client) -->
		<jar destfile="${dist}/dashboardstatus-shared.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/dashboardstatus/ConnectionLogItem.class" />
			<include name="com/mirth/connect/plugins/dashboardstatus/DashboardConnectorStatusServletInterface.class" />
		</jar>

		<!-- Create server jar -->
		<jar destfile="${dist}/dashboardstatus-server.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/dashboardstatus/**" />
			<exclude name="com/mirth/connect/plugins/dashboardstatus/ConnectionLogItem.class" />
			<exclude name="com/mirth/connect/plugins/dashboardstatus/DashboardConnectorStatusServletInterface.class" />
		</jar>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/dashboardstatus-${version}.zip" basedir="${server.extensions}" includes="dashboardstatus/" />
	</target>

</project>
