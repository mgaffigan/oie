<?xml version="1.0"?>
<project name="datatype-json" basedir="." default="build">

	<dirname property="plugin.dir" file="${ant.file.datatype-json}"/>
	<property file="${plugin.dir}/../build.properties" />

	<property name="src" value="${basedir}/src" />
	<property name="classes" value="${basedir}/classes" />
	<property name="dist" value="${server.extensions}/datatype-json" />

	<path id="classpath">
		<fileset dir="${server.lib}" includes="**/*.jar" />
		<pathelement location="${server.classes}" />
		<fileset dir="${client.lib}" includes="**/*.jar" />
		<pathelement location="${client.classes}" />
	</path>

	<target name="clean">
		<delete dir="${classes}" />
	</target>

	<target name="compile">
		<mkdir dir="${classes}" />
		<javac srcdir="${src}" destdir="${classes}" debug="on" includeAntRuntime="false">
			<classpath refid="classpath" />
		</javac>
	</target>

	<target name="build" depends="compile">
		<mkdir dir="${dist}" />
		<copy todir="${dist}" file="${src}/plugin.xml" />
		<jar destfile="${dist}/datatype-json-shared.jar" basedir="${classes}">
			<exclude name="com/mirth/connect/plugins/datatypes/json/JSONDataTypeServerPlugin.class" />
			<exclude name="com/mirth/connect/plugins/datatypes/json/JSONDataTypeClientPlugin.class" />
		</jar>
		<jar destfile="${dist}/datatype-json-server.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/datatypes/json/JSONDataTypeServerPlugin.class" />
		</jar>
		<jar destfile="${dist}/datatype-json-client.jar" basedir="${classes}">
			<include name="com/mirth/connect/plugins/datatypes/json/JSONDataTypeClientPlugin.class" />
		</jar>
	</target>

	<target name="create-zip">
		<zip destfile="${server.dist.extensions}/datatype-json-${version}.zip" basedir="${server.extensions}" includes="datatype-json/**/*" />
	</target>

</project>
